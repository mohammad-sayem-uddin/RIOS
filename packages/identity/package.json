{
  "name": "@rios/identity",
  "version": "0.1.0",
  "private": true,
  "description": "RIOS Identity Domain Foundation — canonical Research Identity domain model placeholders",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "dev": "tsc -b --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rios/shared": "workspace:*"
  },
  "devDependencies": {
    "@rios/typescript-config": "workspace:*",
    "typescript": "^5.6.3"
  }
}
