/**
 * Infrastructure mappers — public API.
 *
 * Exports mapping contracts for bidirectional conversion between
 * persistence entities, database rows, and domain aggregates.
 */
export type { AggregateMapper, RowMapper, SpecificationTranslator } from './aggregate-mapper.js';
