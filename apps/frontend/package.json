{
  "name": "@rios/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "RIOS Frontend — Next.js application",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .turbo .next *.tsbuildinfo",
    "dev": "tsc -b --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rios/shared": "workspace:*"
  },
  "devDependencies": {
    "@rios/typescript-config": "workspace:*",
    "typescript": "^5.6.3"
  }
}
